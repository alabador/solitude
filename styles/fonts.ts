import { Unica_One, Albert_Sans } from "next/font/google";

export const unicaOne = Unica_One({
    weight: '400',
    subsets: ['latin']
});
export const albertSans = Albert_Sans({
    subsets: ['latin']
});